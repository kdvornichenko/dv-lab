---
phase: 09-implement-website-pet-widget
plan: 04
type: execute
title: Pet target rollout, sound guardrails, and browser verification
wave: 3
depends_on:
  - 09-03
files_modified:
  - apps/web/components/AppSidebar.tsx
  - apps/web/components/dashboard/LessonsCalendarPanel.tsx
  - apps/web/components/dashboard/LessonsPanel.tsx
  - apps/web/components/dashboard/PaymentsPanel.tsx
  - apps/web/components/pet/WebsitePetProvider.tsx
  - apps/web/components/pet/pet-browser-checklist.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - API-05
must_haves:
  truths:
    - "The pet can rest only on explicitly marked broad CRM surfaces."
    - "Ambient sound cannot autoplay and remains controlled by the persisted sound setting."
    - "Final verification covers click-through, visible animated WebP assets, reduced motion, privacy pose, mobile behavior, settings persistence, and login exclusion."
  artifacts:
    - path: "apps/web/components/AppSidebar.tsx"
      provides: "Sidebar broad pet target"
      contains: "data-pet-target"
    - path: "apps/web/components/dashboard/LessonsCalendarPanel.tsx"
      provides: "Calendar/lesson broad pet target"
      contains: "data-pet-target"
    - path: "apps/web/components/pet/WebsitePetProvider.tsx"
      provides: "Sound guardrails"
      contains: "soundEnabled"
    - path: "apps/web/components/pet/pet-browser-checklist.md"
      provides: "Manual browser verification checklist"
      contains: "prefers-reduced-motion"
  key_links:
    - from: "apps/web/lib/pet/pet-targets.ts"
      to: "workspace components"
      via: "[data-pet-target] attributes"
      pattern: "data-pet-target"
    - from: "apps/web/app/(workspace)/settings/pet/PetSettingsClient.tsx"
      to: "apps/web/components/pet/WebsitePetProvider.tsx"
      via: "soundEnabled persisted setting"
      pattern: "soundEnabled"
---

# Plan 09-04: Pet target rollout, sound guardrails, and browser verification

<objective>
Mark safe workspace surfaces as pet targets, enforce sound guardrails, and complete final manual/browser verification.
</objective>

<tasks>

<task type="auto">
  <name>Task 09-04-01: Add explicit pet targets to broad workspace surfaces</name>
  <read_first>
    .planning/phases/09-implement-website-pet-widget/09-CONTEXT.md
    apps/web/components/AppSidebar.tsx
    apps/web/components/dashboard/LessonsCalendarPanel.tsx
    apps/web/components/dashboard/LessonsPanel.tsx
    apps/web/components/dashboard/PaymentsPanel.tsx
    apps/web/AGENTS.md
  </read_first>
  <files>apps/web/components/AppSidebar.tsx, apps/web/components/dashboard/LessonsCalendarPanel.tsx, apps/web/components/dashboard/LessonsPanel.tsx, apps/web/components/dashboard/PaymentsPanel.tsx</files>
  <action>
    Before editing existing component symbols, run GitNexus impact for `AppSidebar`, `LessonsCalendarPanel`, `LessonsPanel`, and `PaymentsPanel`; report direct callers and risk. If HIGH or CRITICAL, stop and ask for confirmation.

    Add `data-pet-target` only to broad, non-input workspace surfaces such as sidebar shell/header areas, lesson/calendar panel containers, and payment panel containers.

    Do not add `data-pet-target` to `button`, `input`, `textarea`, table rows/cells, dialog content, or elements with `data-private`. If a candidate surface contains private text, add the target to a neutral wrapper region instead.
  </action>
  <verify>
    <automated>rg -n "data-pet-target" apps/web/components apps/web/app && ! rg -n "<button[^\\n]*data-pet-target|data-private[^\\n]*data-pet-target|data-pet-target[^\\n]*data-private" apps/web/components apps/web/app && yarn --cwd apps/web typecheck</automated>
  </verify>
  <done>At least three broad safe workspace targets exist and no private/control element is marked as a pet target.</done>
</task>

<task type="auto">
  <name>Task 09-04-02: Implement sound guardrails without surprise audio</name>
  <read_first>
    apps/web/components/pet/WebsitePetProvider.tsx
    apps/web/app/(workspace)/settings/pet/PetSettingsClient.tsx
    .planning/phases/09-implement-website-pet-widget/09-CONTEXT.md
  </read_first>
  <files>apps/web/components/pet/WebsitePetProvider.tsx, apps/web/app/(workspace)/settings/pet/PetSettingsClient.tsx</files>
  <action>
    If ambient sound is implemented in this phase, it must be behind `settings.soundEnabled === true`, default false, and only start after a user gesture such as toggling sound in settings.

    If no acceptable audio asset exists, keep the setting persisted and render no audio element. Do not autoplay audio on page load. Do not loop audio by default. If an audio element is added, use a workspace-local pet asset path under `/pets/cat/` and handle rejected `play()` promises with `catch`.
  </action>
  <verify>
    <automated>! rg -n "autoplay|loop=\\{true\\}" apps/web/components/pet/WebsitePetProvider.tsx && rg -n "soundEnabled" apps/web/components/pet/WebsitePetProvider.tsx apps/web/app/(workspace)/settings/pet/PetSettingsClient.tsx && yarn --cwd apps/web typecheck</automated>
  </verify>
  <done>Sound cannot autoplay, is governed by `soundEnabled`, and web typecheck passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 09-04-03: Complete final browser verification checklist</name>
  <read_first>
    .planning/phases/09-implement-website-pet-widget/09-VALIDATION.md
    apps/web/components/pet/WebsitePetProvider.tsx
    apps/web/components/pet/WebsitePetOverlay.tsx
    apps/web/app/(workspace)/settings/pet/PetSettingsClient.tsx
  </read_first>
  <files>apps/web/components/pet/pet-browser-checklist.md, .planning/ROADMAP.md</files>
  <action>
    Create `apps/web/components/pet/pet-browser-checklist.md` with exact manual verification steps: enable pet in settings without refresh and confirm overlay appears; disable it without refresh and confirm overlay hides; refresh and confirm persisted state; confirm pet appears in workspace and not `/login`; click underlying CRM UI while pet is visually over it; confirm active mode walks across the viewport; confirm it jumps only to a visible `data-pet-target`, lands, and rests on that target; emulate `prefers-reduced-motion: reduce`; toggle privacy mode and confirm privacy pose; test mobile viewport reduced movement; confirm representative animated WebP poses/assets for `walk`, `sleep`, and temporary mapped poses are visible and nonblank. Do not require manual frame stepping because animated WebP owns its internal frame loop.

    Run a fast smoke loop first with `yarn --cwd apps/web test:pet`. Then run final commands: `yarn --cwd apps/web test:pet`, `yarn --cwd apps/web typecheck`, `yarn --cwd apps/api test`, and `yarn test`.

    Update `.planning/ROADMAP.md` Phase 9 progress row from `0/0` to the final plan count and status according to execution state.
  </action>
  <verify>
    <automated>yarn --cwd apps/web test:pet && yarn --cwd apps/web typecheck && yarn --cwd apps/api test && yarn test</automated>
  </verify>
  <done>Manual checklist exists and final automated checks pass; human verification can approve live settings, walking, jump-to-target-and-rest, click-through, animated WebP visibility, reduced motion, privacy pose, mobile behavior, settings persistence, and login exclusion.</done>
</task>

</tasks>

<verification>
Run `yarn --cwd apps/web test:pet && yarn --cwd apps/web typecheck && yarn --cwd apps/api test && yarn test`, then complete the browser checklist.
</verification>

<success_criteria>
The pet uses explicit safe targets, sound cannot surprise the user, and final verification covers live settings, walking, jump-to-target-and-rest, click-through, reduced motion, privacy, mobile, settings persistence, login exclusion, and animated WebP visibility.
</success_criteria>
