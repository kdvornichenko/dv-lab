---
phase: 09-implement-website-pet-widget
plan: 02
type: execute
title: Persisted pet settings API and workspace controls
wave: 1
depends_on:
  - 09-01
files_modified:
  - packages/api-types/src/index.ts
  - packages/db/src/schema.ts
  - packages/db/src/settings-repository.ts
  - apps/api/src/routes/settings.ts
  - apps/api/src/services/settings-service.ts
  - apps/api/src/app.test.ts
  - apps/web/lib/crm/api.ts
  - apps/web/lib/pet/pet-settings-store.ts
  - apps/web/app/(workspace)/settings/pet/page.tsx
  - apps/web/app/(workspace)/settings/pet/PetSettingsClient.tsx
autonomous: true
requirements:
  - API-05
must_haves:
  truths:
    - 'The user can enable or disable the pet through an authenticated persisted setting.'
    - 'Sound is controlled by an explicit setting and defaults off.'
    - 'Pet settings use the API/Postgres or memory fallback boundary, not browser-only localStorage.'
    - 'Settings changes are published through a shared web store so the mounted pet provider can react without a page refresh.'
  artifacts:
    - path: 'packages/api-types/src/index.ts'
      provides: 'Pet settings schema and response type'
      contains: 'petSettingsSchema'
    - path: 'apps/api/src/routes/settings.ts'
      provides: 'Authenticated pet settings route'
      contains: '/pet'
    - path: 'apps/api/src/services/settings-service.ts'
      provides: 'Pet settings service methods'
      contains: 'getPetSettings'
    - path: 'apps/web/app/(workspace)/settings/pet/PetSettingsClient.tsx'
      provides: 'Workspace pet settings controls'
      contains: 'soundEnabled'
    - path: 'apps/web/lib/pet/pet-settings-store.ts'
      provides: 'useSyncExternalStore-compatible pet settings store'
      contains: 'subscribePetSettings'
  key_links:
    - from: 'apps/web/app/(workspace)/settings/pet/PetSettingsClient.tsx'
      to: 'apps/web/lib/crm/api.ts'
      via: 'getPetSettings/savePetSettings'
      pattern: 'savePetSettings'
    - from: 'apps/api/src/routes/settings.ts'
      to: 'packages/api-types/src/index.ts'
      via: 'pet settings schema validation'
      pattern: 'petSettingsSchema'
    - from: 'apps/web/app/(workspace)/settings/pet/PetSettingsClient.tsx'
      to: 'apps/web/lib/pet/pet-settings-store.ts'
      via: 'optimistic settings publication after save'
      pattern: 'setPetSettingsSnapshot'
---

# Plan 09-02: Persisted pet settings API and workspace controls

<objective>
Add authenticated persistence and workspace UI controls for pet visibility, sound, and activity level.
</objective>

<tasks>

<task type="auto">
  <name>Task 09-02-01: Add shared pet settings schemas and API route</name>
  <read_first>
    .planning/phases/09-implement-website-pet-widget/09-CONTEXT.md
    .planning/phases/09-implement-website-pet-widget/09-RESEARCH.md
    packages/api-types/src/index.ts
    apps/api/src/routes/settings.ts
    apps/api/src/services/settings-service.ts
    apps/api/src/app.test.ts
  </read_first>
  <files>packages/api-types/src/index.ts, packages/db/src/schema.ts, packages/db/src/settings-repository.ts, apps/api/src/routes/settings.ts, apps/api/src/services/settings-service.ts, apps/api/src/app.test.ts</files>
  <action>
    Before editing existing symbols, run GitNexus impact for `settings-service.ts` setting methods and `settings.ts` route handlers that will be changed; report direct callers and risk. If HIGH or CRITICAL, stop and ask for confirmation.

    Extend shared API types with `petSettingsSchema` containing `enabled: z.boolean().default(true)`, `soundEnabled: z.boolean().default(false)`, and `activityLevel: z.enum(['reduced', 'normal', 'playful']).default('normal')`. Add `petSettingsResponseSchema` returning `{ ok: true, settings: petSettingsSchema }`. Export `PetSettings` and `PetSettingsResponse`.

    Add protected settings endpoints `GET /settings/pet` and `PUT /settings/pet`. `PUT /settings/pet` must validate the body with `petSettingsSchema`.

    Use the existing settings service/repository style and memory fallback. Add DB-backed persistence if `settings-repository.ts` owns similar settings rows; otherwise add a `pet_settings` table in `packages/db/src/schema.ts` scoped to teacher profile with `enabled`, `sound_enabled`, `activity_level`, `created_at`, and `updated_at`.

    Add `apps/api/src/app.test.ts` smoke assertions for `GET /settings/pet` and `PUT /settings/pet`.

  </action>
  <verify>
    <automated>yarn --cwd apps/api test</automated>
  </verify>
  <done>API pet settings schemas, service, route, persistence fallback, and smoke tests are implemented and API tests pass.</done>
</task>

<task type="auto">
  <name>Task 09-02-02: Add web pet settings client and API methods</name>
  <read_first>
    apps/web/lib/crm/api.ts
    apps/web/app/(workspace)/settings/theme/ThemeSettingsClient.tsx
    apps/web/app/(workspace)/settings/sidebar/SidebarSettingsClient.tsx
    apps/web/components/ui/switch.tsx
    apps/web/AGENTS.md
  </read_first>
  <files>apps/web/lib/crm/api.ts, apps/web/lib/pet/pet-settings-store.ts, apps/web/app/(workspace)/settings/pet/page.tsx, apps/web/app/(workspace)/settings/pet/PetSettingsClient.tsx</files>
  <action>
    Add web API methods `getPetSettings(): Promise<PetSettingsResponse>` and `savePetSettings(input: PetSettings): Promise<PetSettingsResponse>` to `apps/web/lib/crm/api.ts`.

    Create `apps/web/lib/pet/pet-settings-store.ts` with a `useSyncExternalStore`-compatible store: `getPetSettingsSnapshot()`, `setPetSettingsSnapshot(settings)`, `subscribePetSettings(listener)`, and `DEFAULT_PET_SETTINGS`. The store is the live bridge between settings UI and the mounted provider; it must not use browser-only localStorage as persistence.

    Create `apps/web/app/(workspace)/settings/pet/page.tsx` and `apps/web/app/(workspace)/settings/pet/PetSettingsClient.tsx`. The settings UI must expose an enable/disable `Switch`, a sound enabled `Switch` defaulting false, and an activity level control with exact values `reduced`, `normal`, `playful`.

    `PetSettingsClient` must load persisted settings into the shared store and publish updates through `setPetSettingsSnapshot` immediately after a successful save, so an already-mounted `WebsitePetProvider` can hide/show the overlay without a refresh once 09-03 is implemented.

    Use concrete operational copy. Do not add marketing language. Do not add raw `overflow-auto`; if scrolling is needed use shadcn `ScrollArea`.

  </action>
  <verify>
    <automated>yarn --cwd apps/web typecheck</automated>
  </verify>
  <done>Workspace pet settings page exists, uses web API methods, exposes enabled/sound/activity controls, publishes saved settings to the shared live store, and web typecheck passes.</done>
</task>

</tasks>

<verification>
Run `yarn --cwd apps/api test && yarn --cwd apps/web typecheck`.
</verification>

<success_criteria>
Pet settings persist through authenticated application boundaries and can be controlled from workspace settings.
</success_criteria>
