# Plan 01-02: Turborepo Workspace Scaffold

## Objective

Replace the single-app layout with Turborepo workspace directories and root configuration.

## Tasks

- [x] Remove legacy active app files.
- [x] Create root `package.json`, `turbo.json`, `tsconfig.base.json`, and formatting/lint configs.
- [x] Create `apps/web` package shell.
- [x] Create `apps/api` package shell.
- [x] Create shared package shells.

## Verification

- [x] Root package scripts delegate to `turbo run`.
- [x] Workspace packages have local scripts.
